# Disabled. PNG icons are already committed. Auto-decode on push is not needed
# and retriggered extra Pages builds.
name: Write PWA PNG icons
on:
  workflow_dispatch:
jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Icons already committed. This workflow does not run on push."
